# Database
DATABASE_URL=postgres://ieuser:iepass@localhost:5432/iemarket?sslmode=disable

# OpenSearch
OPENSEARCH_URL=http://localhost:9200

# MinIO
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=market-data
MINIO_USE_SSL=false

# Stripe (test mode)
STRIPE_SECRET_KEY=sk_test_placeholder

# Claude API (for analyze_data)
ANTHROPIC_API_KEY=

# AWS Bedrock (for embeddings — optional, falls back to local embedder)
AWS_REGION=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# Server
HTTP_PORT=8080
MCP_PORT=8081
